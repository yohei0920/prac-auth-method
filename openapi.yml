openapi: 3.0.0
info:
  title: Practice API
  version: 1.0.0
  description: Rails API実践プロジェクト
servers:
  - url: http://localhost:3000
    description: Development server
paths:
  /api/v1/health:
    get:
      summary: ヘルスチェック
      responses:
        "200":
          description: 成功
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
